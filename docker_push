#!/bin/bash
echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
docker build . --tag devopsexam --build-arg JAR_FILE=./target/travisdemo-0.0.1-SNAPSHOT.jar
docker devopsexam  boylan157/devopsexam
docker boylan157/devopsexam